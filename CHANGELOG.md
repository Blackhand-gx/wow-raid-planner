# 版本记录

## v1.0.183 (2026-05-30)

- 调整鼠标滚轮缩放比例：ZOOM_SPEED 从 0.001 提升至 0.02，每次滚轮刻度缩放约 6%（原 0.3%）
- 现在滚轮缩放速度明显可感知，与工具栏按钮缩放（30%/档）形成合理梯度

## v1.0.167 (2026-05-30)

- 修复首领图片在保存文件/自动恢复/打开文件时丢失的问题
- Boss 类型新增 iconPath 字段，MapData 类型新增 mapPath 字段，保存预设路径用于恢复
- serializeState/takeSnapshot：有预设路径时剥离 base64 imageDataUrl（减小文件体积），保留路径字段
- 新增 restoreImages 工具：从 iconPath/mapPath 异步重新加载图片
- openFile 和 useAutoSave 恢复后自动从路径重建图片，无路径的自定义上传图片则显示占位图标


## v1.0.152 (2026-05-26)

- 未填充圆形/矩形使用 hitFunc 限制命中区域仅为边框，内部区域点击和拖拽均可穿透到下层元素
- 绘图工具点击已有元素时自动切换为选择工具并选中该元素
- 绘图/标记操作时自动切换到标注标签页
- 层级滑块定位优化：右侧溢出自动翻转、边界裁剪、修复移动元素后位置不同步

## v1.0.137 (2026-05-26)

- 全局层级控制改为画布浮动滑块：选中元素后在其左侧出现拖拽滑块，直观调整层级
- 滑块拖拽时实时更新画布层级，无需松手即可预览效果
- 点击轨道空白处直接跳转到对应层级
- 修复新团员生成位置：从渐进偏移改为中心附近随机散布
- 修复拖拽元素时 renderOrder 快照缺失导致撤销异常
- 修复滑块 z-index 过高遮挡 MRT 导入对话框
- 滑块颜色指示层级位置：绿色（顶层）→ 黄色（中层）→ 红色（底层）

## v1.0.103 (2026-05-26)

- 修复拖拽元素时碰到其他元素就停止的问题：拖拽改为 window 级事件监听，鼠标跨越其他元素不会中断
- 新增全局元素层级系统：统一渲染层 + renderOrder 跨类型排序
- 选中任意元素后，侧边栏底部出现 ▲▼ 全局层级控制条
- 各面板列表内 ▲▼ 按钮同步影响画布全局层级

## v1.0.99 (2026-05-24)

- 所有预设首领补全专属地图和头像图标（9 个首领共 9 张地图 + 15 个头像）

## v1.0.98 (2026-05-24)

- 预设地图加载后默认锁定，防止误拖拽

## v1.0.97 (2026-05-24)

- 修复预设选择累积问题：选择新首领时弹窗确认，确认后清除原有首领和地图再添加

## v1.0.96 (2026-05-24)

- 预设按钮三合一：版本/副本/首领合并为单一下拉菜单
- 下拉菜单中版本和副本不可选（灰色文字），仅首领可点击
- 缩进显示层级：版本 0 缩进 → 副本 1 级缩进 → 首领 2 级缩进
- 选中首领直接添加，无需再点 + 按钮
- 移除不再使用的 presetExpIdx / presetRaidIdx / presetBossIdx 状态

## v1.0.95 (2026-05-24)

- 新增地图锁定功能：锁定后地图不可拖拽移动，防止误操作
- 新增地图居中功能：一键将地图居中到画布视口
- 地图数据增加 `locked` 字段，恢复旧存档自动兼容

## v1.0.94 (2026-05-24)

- 职责图标与团队标记放大（标记 60%、职责 50% 团员图标大小），下半部覆盖团员图标
- 职责图标按标准魔兽布局重新提取（坦克=盾牌、治疗=十字、输出=剑）
- 修复缩放百分比点击回到 100% 功能

## v1.0.93 (2026-05-24)

- 新增团员职责图标功能：可为团员分配坦克/治疗/输出职责，显示在图标上方与团队标记并行
- 团员编辑窗增加职责选择器：三个职责图标按钮（坦克盾牌/治疗十字/输出剑）
- 团员列表面板行内显示职责图标
- 下载并集成魔兽世界官方角色定位图标（UI-LFG-ICON-ROLES 纹理提取）

## v1.0.92 (2026-05-24)

- 修复预存地图加载 404：中文文件名改为 ASCII 命名，解决 IIS URL 编码兼容问题

## v1.0.91 (2026-05-24)

- 修复预存地图静默加载失败：toDataURL 改用 PNG 编码以提升兼容性
- 预存地图/首领图标加载失败时输出 console.error 便于排查

## v1.0.87 (2026-05-24)

- 新增团员团队标记功能：每个团员可分配一个标记，显示在图标上方
- 新增地图标记工具：工具栏增加标记工具，点击画布直接放置标记
- 新增标记大小调节：侧边栏可统一调节所有地图标记的大小（8~64px）
- 团员编辑窗增加标记选择器：8 个标准魔兽团队标记图标按钮
- 团员列表面板行内显示当前标记图标
- 标记标注支持拖拽、橡皮擦删除、撤销/重做

## v1.0.81 (2026-05-24)

- 修复地图自动适配全屏：上传或预设地图自动缩放至画布大小
- 修复撤销/重做功能：全面接入快照捕获体系
- 新增首领默认圆形头像：威厄高尔、艾佐拉克
- 新增版本号显示：右下角状态栏 `v1.0.xx by 黑手`
- 修复首领头像裁剪为圆形
- 修复有头像时骷髅 emoji 隐藏
